tarai x y z = {
    puts ("tarai " + sof x + " " + sof y + " " + sof z)
    if x <= y then
        y
    else
        tarai (tarai (x-1) y z) (tarai (y-1) z x) (tarai (z-1) x y)
}

_ = puts (sof (tarai 8 7 0))

