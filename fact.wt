fact : int -> int
fact n =
    if n < 1 then 1
    else n * fact (n-1)

loop : int -> int -> unit
loop s e =
    if s < e then {
        puts ("fact " + sof s + " = " + sof (fact s))
        loop (s+1) e
    }

_ = {
    // modules()
    loop 1 10
}
