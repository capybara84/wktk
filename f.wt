
module Graphics2D
type 'a pt = {
    x : 'a
    y : 'a
}
get_x p = p.x
get_y p = p.y

module Graphics3D
type 'a pt = {
    x : 'a
    y : 'a
    z : 'a
}
get_x p = p.x
get_y p = p.y
get_z p = p.z

module Main
_ =
    let p = Grapihcs3D.pt { x = 1; y = 2; z = 3 } in
    prn (get_y p)

