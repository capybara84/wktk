tak x y z = {
//    puts ("tak " + sof x + " " + sof y + " " + sof z)
    if x <= y then
        z
    else
        tak (tak (x-1) y z) (tak (y-1) z x) (tak (z-1) x y)
}

_ = puts (sof (tak 12 6 0))

